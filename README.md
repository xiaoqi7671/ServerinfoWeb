# 🎮 Minecraft 服务器信息看板 (CF Pages 版)
# 🎮 Minecraft Server Dashboard (CF Pages Edition)

[**English**](#english-version) | [**中文**](#chinese-version)

---

## ⚠️ Disclaimer / 免责声明
> **AI Generation Notice:** This project is entirely generated by AI. It may contain bugs or security vulnerabilities.
> **AI 制作声明：** 本项目完全由 AI 制作，可能包含逻辑错误或安全漏洞，请在正式环境使用前自行检查。

---

<div id="chinese-version"></div>

## 📖 Minecraft 服务器信息看板 (中文版本)

### 项目介绍
本项目专为 **Cloudflare Pages** 开发，通过 Pages Function 读取 CF KV 存储系统中的配置。
**注意：** 本项目必须部署在 Cloudflare 环境中，不支持纯静态本地运行。

### 1. 准备 KV 数据库
1. 登录 Cloudflare 控制台 -> **Workers & Pages** -> **KV**。
2. 创建命名空间（推荐名：`SERVER_DB`）。
3. 添加 Key，名称设为 `config`。

### 2. 配置数据
在 `config` 的 Value 中填入以下 JSON 数据（注意：复制后请自行补全结尾的反引号）：

```json
{
  "serverName": "你的服务器名称",
  "java": {
    "ip": "java.example.com",
    "port": "25565",
    "version": "1.20.1"
  },
  "bedrock": {
    "ip": "be.example.com",
    "port": "19132",
    "version": "最新版"
  },
  "features": [
    {
      "title": "🛡️ 安全保障",
      "content": "这里填入特色描述",
      "styleClass": "feature-blue"
    }
  ],
  "contact": [
    {
      "note": "💬 官方群号",
      "value": "12345678"
    }
  ]
```
### 3. 绑定与部署
1. 将 `index.html` 上传至 Cloudflare Pages。
2. 进入 Pages **设置** -> **函数** -> **KV 命名空间绑定**。
3. 将变量名 `SERVER_DB` 绑定到你的 KV 空间。

---

<div id="english-version"></div>

## 📖 Minecraft Server Dashboard (English Version)

### Introduction
Designed for **Cloudflare Pages**, utilizing CF KV storage.
**Note:** Standalone local deployment is not supported.

### 1. Setup KV
1. Cloudflare Console -> **Workers & Pages** -> **KV**.
2. Create namespace (e.g., `SERVER_DB`).
3. Add Key named `config`.

### 2. Configuration
Fill `config` value with this JSON (Note: Please manually add the closing backticks after pasting):

```json
{
  "serverName": "Your Server Name",
  "java": {
    "ip": "java.example.com",
    "port": "25565",
    "version": "1.20.1"
  },
  "bedrock": {
    "ip": "be.example.com",
    "port": "19132",
    "version": "Latest"
  },
  "features": [
    {
      "title": "🛡️ Security",
      "content": "Feature description",
      "styleClass": "feature-blue"
    }
  ],
  "contact": [
    {
      "note": "💬 Community Group",
      "value": "12345678"
    }
  ]
```
### 3. Binding
1. Upload `index.html` to Cloudflare Pages.
2. Go to **Settings** -> **Functions** -> **KV namespace bindings**.
3. Bind `SERVER_DB` to your KV namespace.

---

### 🎨 Style Classes / 样式类
`feature-blue`, `feature-green`, `feature-orange`, `feature-red`, `feature-gray`, `feature-purple`, `feature-gold`, `feature-cyan`, `feature-pink`.

---
*Generated by Gemini AI*
