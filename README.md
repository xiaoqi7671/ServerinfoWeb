🎮 Minecraft Server Dashboard (CF Pages Edition)
> [!IMPORTANT]
> AI Generation Notice / AI 制作声明
> This project is entirely generated by AI. It may contain bugs or security vulnerabilities. Please use it with caution.
> 本项目完全由 AI 制作，可能包含逻辑错误或安全漏洞，请在正式环境使用前自行检查。
> 
<details>
<summary><b>📖 点击展开中文部署手册 (Chinese Version)</b></summary>
项目介绍
本项目专为 Cloudflare Pages 开发，依赖 CF KV 存储系统。
注意： 本项目无法直接在本地作为纯静态网页运行，必须部署在 Cloudflare 环境中。
部署指南
1. 准备 KV 数据库
 * 登录 Cloudflare 控制台 -> Workers & Pages -> KV。
 * 创建一个命名空间（推荐命名为 SERVER_DB）。
 * 在该空间内添加一个 Key，名称设为 config。
2. 配置数据
在 config 的值中填入以下 JSON 模板并根据实际修改：
{
  "serverName": "你的服务器名称",
  "java": {
    "ip": "java.example.com",
    "port": "25565",
    "version": "1.20.1"
  },
  "bedrock": {
    "ip": "be.example.com",
    "port": "19132",
    "version": "最新版"
  },
  "features": [
    {
      "title": "🛡️ 安全保障",
      "content": "这里填入你的特色描述",
      "styleClass": "feature-blue"
    }
  ],
  "contact": [
    {
      "note": "💬 官方群号",
      "value": "12345678"
    }
  ]
}

提示：如果不需要显示 Java 或 基岩版，将对应字段设为 null。
3. 绑定命名空间
 * 将 index.html 上传至 Cloudflare Pages。
 * 进入 Pages 的 设置 (Settings) -> 函数 (Functions) -> KV 命名空间绑定。
 * 将变量名 SERVER_DB 绑定到你刚才创建的 KV 空间。
🎨 可选颜色类
在 styleClass 中可填入：feature-blue, feature-green, feature-orange, feature-red, feature-gray, feature-purple, feature-gold, feature-cyan, feature-pink。
</details>
<details>
<summary><b>📖 Click to expand English Guide (英文版本)</b></summary>
Introduction
This project is designed specifically for Cloudflare Pages and relies on Cloudflare KV storage.
Note: It cannot be deployed as a standalone static site locally.
Deployment Guide
1. Setup KV Database
 * Log in to Cloudflare -> Workers & Pages -> KV.
 * Create a namespace (e.g., SERVER_DB).
 * Add a Key named config.
2. Configure Data
Fill the config value with the following JSON template:
{
  "serverName": "Your Server Name",
  "java": {
    "ip": "java.example.com",
    "port": "25565",
    "version": "1.20.1"
  },
  "bedrock": {
    "ip": "be.example.com",
    "port": "19132",
    "version": "Latest"
  },
  "features": [
    {
      "title": "🛡️ Security",
      "content": "Describe your server feature here",
      "styleClass": "feature-blue"
    }
  ],
  "contact": [
    {
      "note": "💬 Community Group",
      "value": "12345678"
    }
  ]
}

Note: Set java or bedrock to null to hide the specific card.
3. Binding
 * Upload index.html to Cloudflare Pages.
 * Go to Settings -> Functions -> KV namespace bindings.
 * Bind the variable name SERVER_DB to your created KV namespace.
🎨 Style Classes
Available styleClass: feature-blue, feature-green, feature-orange, feature-red, feature-gray, feature-purple, feature-gold, feature-cyan, feature-pink.
</details>
Generated by Gemini AI
